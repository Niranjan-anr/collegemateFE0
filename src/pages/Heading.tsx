import { Subheading } from '../components/subHeading';
import { HomePagebutton } from '../components/ButtonatHomepage';

const Heading = () => {
  return (
    <section>
      <Subheading title={'Welcome!'}>to collegemate</Subheading>
      <HomePagebutton />
    </section>
  );
};

export default Heading;
